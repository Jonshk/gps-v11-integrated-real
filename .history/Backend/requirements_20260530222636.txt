# requirements.txt  (versión completa final)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Va en: Backend/requirements.txt  (REEMPLAZAR)

# ── Core ──────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-dotenv==1.0.1

# ── Base de datos ─────────────────────────────────────────────
psycopg[binary]==3.1.19

# ── Seguridad ─────────────────────────────────────────────────
bcrypt==4.1.3
slowapi==0.1.9           # rate limiting alternativo (opcional, usamos el nuestro)

# ── HTTP client ───────────────────────────────────────────────
httpx==0.27.0

# ── Reportes ─────────────────────────────────────────────────
reportlab==4.1.0
openpyxl==3.1.2

# ── Redis (para WebSockets multi-instancia) ───────────────────
# Solo necesario si configuras REDIS_URL en .env
redis==5.0.4

# ── Logging / Monitoreo (opcionales) ─────────────────────────
# Descomentar según lo que uses:
# sentry-sdk[fastapi]==2.5.1
# logtail-python==0.3.3

# ── Firebase (push notifications, opcional) ───────────────────
# firebase-admin==6.5.0