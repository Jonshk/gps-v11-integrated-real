services:
  - type: web
    name: gps-v11-integrated-real
    runtime: python
    pythonVersion: "3.11.9"
    rootDir: Backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000